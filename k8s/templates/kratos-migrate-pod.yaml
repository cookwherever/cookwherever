apiVersion: v1
kind: Pod
metadata:
  annotations:
    kompose.cmd: ./kompose -f docker-compose.yaml -f docker-compose.prod.yaml convert -c -o k8s
    kompose.version: 1.26.1 (a9d05d509)
  creationTimestamp: null
  labels:
    io.kompose.service: kratos-migrate
  name: kratos-migrate
spec:
  containers:
    - args:
        - -c
        - /config/kratos/config.yaml
        - migrate
        - sql
        - -e
        - --yes
      env:
        - name: DSN
          value: postgres://postgres:secretpgpassword@postgres:5432/cookwherever
      image: oryd/kratos:v0.9.0-alpha.3
      name: kratos-migrate
      resources: {}
      volumeMounts:
        - mountPath: /config/kratos
          name: kratos-migrate-claim0
  restartPolicy: OnFailure
  volumes:
    - name: kratos-migrate-claim0
      persistentVolumeClaim:
        claimName: kratos-migrate-claim0
status: {}
